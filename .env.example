# Canonical origin for this deployment — no trailing slash.
# Drives <link rel="canonical">, Open Graph URLs, and sitemap.xml.
# If unset, the app falls back to http://localhost:3000, which is correct for
# local dev and wrong for anything else.
NEXT_PUBLIC_SITE_URL=https://example.com
